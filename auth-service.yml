spring:
  application:
    name: auth-service

  datasource: jdbc:postgresql://localhost:5432/auth_db
  username: postgres
  password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8081



